apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: movie-rec-bert-crawl
  namespace: {{ .Values.namespace }}
spec:
  predictor:
    containers:
      - name: kserve-container
        image: konglsh96/movie-rec-bert:crawl
        resources:
          limits:
            cpu: {{ .Values.moviecrawl.resources.limits.cpu }}
            memory: {{ .Values.moviecrawl.resources.limits.memory }}
          requests:
            cpu: {{ .Values.moviecrawl.resources.requests.cpu }}
            memory: {{ .Values.moviecrawl.resources.requests.memory }}
